name: my_first_workflow_push_automation

on: [push]

jobs:
  my_first_job:
    runs-on: ubuntu-latest
    steps:
      - name: print welcome message
        run: |
              cat ls 
              cat ls -a 
